<h1>Artículos</h1>
<div class="row">
	<% @articles.each do |article| %>
		<div class="col-md-4">
			<h2><%= link_to article.title, article %></h2>
			<p>
				<%= truncate(Sanitize.fragment(article.body, Sanitize::Config::RELAXED).html_safe, length:10) %>
			</p>
		</div>
	<% end %>
</div>
<%= will_paginate @articles %>