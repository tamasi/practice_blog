<div class="collapse navbar-collapse">
  <ul class="nav nav-pills navbar-nav">
    
      <li class="dropdown active">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              Blog 
              <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
              <li><a href="#">Blog</a></li>
              <li><a href="#">Blog Detail</a></li>
          </ul>
      </li>
      <li>
          <a href="">About</a>
      </li>
      <% if !current_user.nil? %>
        <li class="dropdown active">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <%= current_user.avatar.exists? ? image_tag(current_user.avatar, class: "user-topnav-icon") : image_tag(current_user.gravatar, class: "user-topnav-icon" ) %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to edit_user_registration_path do %>
                <i class="fas fa-user"></i> Edita tu Perfil
              <% end %>
            </li>
            <li>
              <%= link_to new_article_path do %>
                <i class="fas fa-file-contract"></i> Subí un Artículo
              <% end %>
            </li>
            <li role="separator" class="divider"></li>
              <% if !current_user.nil? && current_user.is_admin? %>
                <li>
                  <%= link_to dashboard_path do %>
                    <i class="fas fa-user-tie"></i> Admin Dashboard
                  <% end %>
                </li>
              <% end -%>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <li><%= link_to "Login", new_user_session_path %></li>
      <% end %>                       
  </ul>
</div>